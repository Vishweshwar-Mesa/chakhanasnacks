# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.shopify
node_modules
*.zip

# Local Claude Code tool config (not project source), except the
# project-wide settings.json (hooks/permissions meant to be shared
# across accounts/machines via git)
.claude/*
!.claude/settings.json

# Non-asset reference files that sometimes land in assets/product-images/
assets/product-images/*.pdf
assets/product-images/*.psd
assets/product-images/*.ai